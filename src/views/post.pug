extends base

block meta
  title= post.seoTitle
  meta(name="description" content=post.seoDesc)
  meta(name="tags" content=post.tags.join())

block content
  h3= post.title
  small Posted #{moment(post.created).format('MMM d, YYYY')}
  div= post.content