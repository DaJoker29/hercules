extends base

block content
  each post in posts
    div
      a(href="/blog/" + post.postID)
        h3 #{post.title}
      small #{ post.author.displayName ? post.author.displayName : post.author.username} &bull; #{moment(post.created).format('MMMM d, YYYY')}
      p #{post.seoDesc}
      hr